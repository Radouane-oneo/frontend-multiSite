<?php
	use printconnect\Carts\Factory;
	/**
	* Implements hook_block_info().
	*/
	function pcstaticproducts_block_info() {
		$blocks = array();
		$blocks['pcstaticproducts'] = array(
			'info' => t('Static Products'),
			'visibility' => 1,
			'pages' => 'cart',
			'region' => 'content',
			'weight' => 13,
			);
		return $blocks;
	}

	/**
	* Implements hook_block_view().
	*/
	function pcstaticproducts_block_view($delta='') {
		$cart = Factory::Current();
		if (!$cart||count($cart->orderItems)==0) {
			$block = array();
			switch ($delta) {
				case 'pcstaticproducts':
					$block['subject'] = '';
					$block['content'] = array( '#theme' => 'pcstaticproducts_block', );
					break;
			}
			return $block;
		}
	}

	/**
	* Implements hook_theme().
	*/
	function pcstaticproducts_theme($existing, $type, $theme, $path){
		return array(
			'pcstaticproducts_block' => array(
				'template' => 'static-products',
			),
		);
	}
